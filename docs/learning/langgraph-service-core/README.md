# LangGraph Service Core 学习导航

这是服务集成最小主线，目标是把 LangGraph SDK 能力稳定落到 FastAPI + SSE。

## 阅读顺序

00. `00-overview.md`（目标与边界）
01. `01-api-contract.md`（最小接口契约）
02. `02-sse-consume-model.md`（前端消费模型）
03. `03-error-and-retry.md`（错误与重试）
04. `04-verification.md`（验收与回归）

## 与 SDK 主线关系

- SDK 主线继续放在 `docs/learning/langgraph-sdk/`。
- 本目录只关注服务落地，不扩展产品化工程主题。
